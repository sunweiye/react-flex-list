import React, {Component} from 'react';
import PropTypes from "prop-types";

// TODO
class AsyncList extends Component {
    render() {
        return (
            <div>
                This is the async list.
            </div>
        );
    }
}

export default AsyncList;
